# ──────────────────────────────────────────────────────────────────────
# Indonesian VSR — Sentence-level Lip Reading
#
# Tested on: Ubuntu 22.04 (or WSL2 Ubuntu), Python 3.10, CUDA 12.4
# GPU required (NVIDIA, CUDA-capable)
#
# Installation:
#     pip install -r requirements.txt
#
# If mamba-ssm or causal-conv1d fail with build errors:
#     pip install causal-conv1d==1.6.1 --no-build-isolation
#     pip install mamba-ssm==2.3.1 --no-build-isolation
# ──────────────────────────────────────────────────────────────────────

# PyTorch (CUDA 12.4 build)
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Scientific computing
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3

# Computer Vision
opencv-python==4.9.0.80
mediapipe==0.10.21

# Mamba (State Space Model)
# Install order: causal-conv1d must be available before mamba-ssm
# Both may need --no-build-isolation
causal-conv1d==1.6.1
mamba-ssm==2.3.1

# Training utilities
tqdm==4.67.3
matplotlib==3.10.8
torchinfo==1.8.0

# Optional: experiment tracking and Jupyter
# Uncomment if needed
# tensorboard==2.20.0
# wandb==0.26.0
# jupyter==1.1.1