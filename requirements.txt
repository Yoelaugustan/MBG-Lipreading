# Use nvcc (CUDA Toolkit) version 12.4.1: conda install cuda -c nvidia/label/cuda-12.4.1
# Use PyPI as the primary index and add PyTorch wheels as an extra index
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1
numpy==1.26.4
pandas>=1.3.0

# Computer Vision
opencv-python>=4.5.0,<4.10.0
mediapipe==0.10.21
albumentations>=1.3.0

# Mamba (State Space Model)
# Note: These usually require --no-build-isolation during pip install, also requires nvcc CUDA toolkit, and C++ compiler g++
mamba-ssm==2.2.2
causal-conv1d==1.4.0

# Training utilities
tqdm>=4.65.0
matplotlib>=3.5.0
seaborn>=0.12.0
torchinfo>=1.8.0

# Optional/Experiment Tracking
tensorboard>=2.10.0
wandb>=0.13.0

# Development
jupyter>=1.0.0
ipywidgets>=8.0.0