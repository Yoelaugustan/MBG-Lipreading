# Use nvcc (CUDA Toolkit) version 12.4.1: conda install cuda -c nvidia/label/cuda-12.4.1
# Use PyPI as the primary index and add PyTorch wheels as an extra index
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Scientific computing
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3

# Computer Vision
opencv-python==4.9.0.80
mediapipe==0.10.21
albumentations>=1.3.0

# Mamba (State Space Model)
# Note: These usually require --no-build-isolation during pip install, also might require nvcc CUDA toolkit, C++ compiler g++, and ninja-build
# causal-conv1d==1.6.1
# mamba-ssm==2.3.1

# Training utilities
tqdm==4.67.3
matplotlib==3.10.8
torchinfo==1.8.0

# Optional: experiment tracking and Jupyter
# Uncomment if needed
# tensorboard==2.20.0
# wandb==0.26.0
# jupyter==1.1.1